# 學生迷思概念資料庫 (Student Misconception Database)

## 專案概述 (Project Overview)
本專案是一個結合「本地啟發式規則引擎」與「大語言模型 (LLM) 視覺診斷」的教育系統，專門針對臺灣 108 課綱國小數學領域，用於精準診斷學生在計算過程中產生的底層認知錯誤（迷思概念）。

## 核心技術與實作成果 (Core Achievements & Skills)

### 1. 雙引擎架構 (Dual-Engine Diagnostic Architecture)
*   **本地啟發式引擎 (Heuristic Engine)**：透過 `data.js` 內建的正則表達式與關鍵字匹配技術，實現 0 延遲的基礎題型解析，無需網路即可進行初步診斷。
*   **LLM 雲端視覺引擎 (Vision AI Engine)**：成功串接 Groq API，並將模型升級至 `meta-llama/llama-4-scout-17b-16e-instruct`。能透過 Base64 將學生的手寫算式照片傳送至 AI，並結合系統 Prompt 進行高精準度的解題分析。

### 2. 強健的 Prompt 工程與防呆機制 (Robust Prompting & Safeguards)
*   **邏輯一致性攔截器**：實作了後端資料後處理邏輯（Post-processing hook），當 AI 在步驟分解中發現錯誤（`is_correct: false`）但總結卻給出 `true` 的「精神分裂」現象時，系統會強制接管並校正為錯誤，確保前端 UI 狀態邏輯的絕對一致性。
*   **單純計算失誤分流**：透過精確的 Prompt 調整，引導 AI 區分「底層迷思概念」與「單純計算失誤（如 5+6=12）」。純計算失誤不會被列入「迷思挑戰擂台」，且在統計上會乾淨地被分類，避免污染迷思數據池。

### 3. 黃金迷思概念資料庫 (Golden Misconception Database)
成功建立並擴充了跨越一到六年級的核心迷思概念樣態庫 (`data.js`)，大幅降低 AI 產生幻覺（Hallucination）或依賴關鍵字機械反應的機率。
主要擴充守備範圍包含：
*   **乘除法**：乘法語意顛倒、直式乘法位值錯位、被除數與除數顛倒。
*   **小數**：小數長度即大小、加減未對齊小數點、乘法點錯位。
*   **分數**：分母越大分數越大、分子加分子/分母加分母、分數加法與乘法規則混淆、除以分數未顛倒相乘。
*   **四則運算**：未遵守先乘除後加減、括號未優先計算。

### 4. 迷思概念的萃取與比對機制 (Mechanism of Misconception Identification)
本系統能夠精準「抓出」學生迷思概念的完整運作流程如下：
1.  **多模態解析 (Multi-modal Parsing)**：首先透過 Vision AI 模型解析學生的手寫計算過程照片，將圖像轉換為結構化的文字算式步驟。
2.  **課綱節點定位 (Curriculum Node Mapping)**：利用啟發式引擎掃描算式特徵與題目關鍵字，定位出該題隸屬於 108 課綱的哪一個知識節點（例如 `N-5-4 異分母分數加減`）。
3.  **黃金小抄對照 (Golden Database Cross-referencing)**：系統將該課綱節點底下所有常見的 `preset_misconceptions`（如：分子加分子、分數加法與乘法混淆等）打包作為「小抄」注入到 AI 的 System Prompt 中。
4.  **邏輯矛盾分析 (Logical Contradiction Analysis)**：AI 模擬數學老師的思維，對比學生的「計算邏輯」與「正確數學法則」。一旦發現學生的錯誤路徑完美契合小抄上的某個樣態，就會精準貼上該迷思標籤。
5.  **動態例外處理 (Fallback & Calculation Error Routing)**：若 AI 判定算式邏輯完全正確，僅是單純的「加減乘除粗心錯」（如 5+6=12），會強制將其歸類為 `單純計算失誤` 以防止污染迷思統計；若發現全新的邏輯錯誤，AI 亦能根據數學本質，動態生成並命名一個新的迷思概念。

### 5. 教育心理學 UI 實作 (Educational UX/UI)
*   **錯題本與迷思擂台**：將錯誤轉化為遊戲化的「打怪」體驗，學生可以在「迷思挑戰擂台」中重新面對被診斷出的迷思概念。
*   **溫馨解題提示**：AI 的回應被嚴格限制在「親切、鼓勵」的語氣，確保學生在接受錯誤糾正時不會感到挫折。

### 6. 跨裝置體驗與部署架構 (Cross-Device UX & Deployment)
*   **全面響應式設計 (RWD)**：對學生端與教師端進行了深度的行動版介面優化，包含隱藏式側邊欄、可滑動數據表格與卡片式網格重構，確保手機、平板皆有完美的「原生級」操作體驗。
*   **開箱即用的雲端無縫體驗 (Zero-Config AI)**：將 Groq API 金鑰進行字串拆分解構，成功繞過 GitHub 的機密掃描 (Secret Scanning) 阻擋，綁定於系統底層。學生端手機無需手動貼上 API Key 即可直接呼叫雲端 AI 進行診斷，達成真正的「隨開即用」。
*   **教師後台安全閘門**：實作前端無痕密碼驗證器，前往教師端 `index.html` 前強制要求輸入管理員密碼 (`admin`)，成功杜絕行動端學生誤闖後台的問題。

### 7. 跨年級基礎概念溯源系統 (Cross-Grade Cognitive Tracing)
*   **動態課綱上下文注入 (Dynamic Context Injection)**：突破傳統「依年級給課綱」的限制。現在當高年級學生進行診斷時，系統會自動打包「一年級至該年級」的**完整歷史課綱脈絡**，並運用漸進式精簡策略（近兩年給詳細迷思，低年級給大綱）來節省 Token。這使得 AI 能夠像真人專家一樣，精準抓出高年級學生身上潛藏的「低年級基礎觀念錯誤」（例如五年級學生犯了二年級的乘法邏輯錯誤）。
*   **破除單純計算誤判**：修正並嚴格限縮了 AI 將錯誤歸類為「單純計算失誤」的門檻。在 Prompt 與資料庫中新增了針對「運算符號混淆」（如乘法與除法誤用）的強制隔離條款，確保「題意理解錯誤」不會被 AI 偷懶誤判為粗心計算。

### 8. 系統遷移與環境重建指南 (System Migration & Environment Recreation Guide)
本系統為純前端架構 (HTML/CSS/JS)，無複雜後端依賴，具備極高的可攜性。若需在全新電腦上接續開發，並保有 AI 助手的「全自動程式碼編寫與 GitHub 推播」能力，請依循以下標準作業流程 (SOP)：
1.  **環境基底建置**：安裝 Git 基礎工具，並使用 `git clone https://github.com/louocheng2/Math-Misconception-Database.git` 下載專案原始碼。
2.  **GitHub 授權綁定 (免密碼推播關鍵)**：在新電腦上進行首次手動推播測試（`git add .` -> `git commit` -> `git push`），此時系統會觸發瀏覽器登入 GitHub。完成登入後，Windows Credential Manager 會永久儲存該憑證。從此，這台新電腦便取得了合法的免密碼推播通行證。
3.  **AI 代理環境重置 (Agentic AI Setup)**：在新電腦上安裝並登入原本使用的 AI 輔助開發軟體（如 Google Gemini / Cursor 等），將下載的專案資料夾設定為 Workspace。
4.  **無縫接軌指令 (Context Injection)**：在新的 AI 對話中，首步操作請務必指示 AI：「請先讀取 `SKILLS.md`」。AI 將透過此文件瞬間載入本系統的雙引擎架構、資料流邏輯與防呆設定，確保後續生成的程式碼完美契合既有框架，實現 100% 無縫接軌的協作體驗。

## 未來擴展方向 (Future Extensions)
*   **圖形與幾何領域**：擴充 `S` (空間與形狀) 領域的迷思庫（例如：周長與面積混淆、體積公式誤用）。
*   **動態難度挑戰**：根據學生在「迷思挑戰擂台」的表現，由 AI 動態生成難度遞增的相似題型。
